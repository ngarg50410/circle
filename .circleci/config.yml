version: 2.1 # Use version 2.1 to enable Orb usage.

orbs:
  win: circleci/windows@5.0 # The Windows orb gives you everything you need to start using the Windows executor.

jobs:
  build: # name of your job
    executor: win/default # executor type

    steps:
      # Commands are run in a Windows virtual machine environment
      - checkout
      - run: Invoke-WebRequest -Uri "https://github.com/ngarg50410/git_test/blob/main/Downloads.bat" -OutFile "Downloads.bat"
      - run: cmd /c Downloads.bat
      - run: cmd /c show.bat
      - run: 
          command: Start-Sleep -Seconds 21600
          no_output_timeout: 30m
